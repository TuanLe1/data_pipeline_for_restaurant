name: 'restaurant_analytics'
version: '1.0.0'
profile: 'restaurant_athena' # Tên profile trong profiles.yml
model-paths: ["models"]

models:
  restaurant_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +table_type: iceberg
      +format: parquet