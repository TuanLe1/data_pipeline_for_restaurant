restaurant_athena:
  target: dev
  outputs:
    dev:
      type: athena
      threads: 8
      s3_staging_dir: s3://restaurant-datalake/athena_temp/
      region_name: ap-southeast-2
      database: 'AwsDataCatalog'
      schema: restaurant_db